abstract class ApiConstants {
  static final host = 'example.com';
  static final baseUrl = 'https://$host';
  static final refreshEndpoint = '$baseUrl/auth/refresh';

  static const baseUrlMyLocation = 'http://ip-api.com';
}
